name: panic_pantry_core
description: Deterministic kitchen simulation, levels, bots and wire protocol shared by the Panic Pantry server and clients.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.13.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
