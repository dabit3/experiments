name: panic_pantry
description: Panic Pantry - a cooperative kitchen-chaos game for web, iOS, Android and macOS.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.13.3

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.38.2
  panic_pantry_core:
    path: ../core
  web_socket_channel: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
