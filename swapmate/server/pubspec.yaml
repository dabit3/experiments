name: swapmate_server
description: Authoritative Swapmate (bughouse) game server speaking JSON over WebSocket.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.13.3

dependencies:
  shelf: ^1.4.2
  shelf_web_socket: ^3.0.0
  web_socket_channel: ^3.0.3
  swapmate_core:
    path: ../packages/swapmate_core

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
