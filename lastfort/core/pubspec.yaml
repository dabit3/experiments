name: lastfort_core
description: Shared deterministic simulation, protocol and content for Lastfort.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.6.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
