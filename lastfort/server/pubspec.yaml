name: lastfort_server
description: Authoritative Lastfort match server (rooms, bots, WebSocket protocol).
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  lastfort_core:
    path: ../core
  shelf: ^1.4.0
  shelf_static: ^1.1.3
  shelf_web_socket: ^3.0.0
  web_socket_channel: ^3.0.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
