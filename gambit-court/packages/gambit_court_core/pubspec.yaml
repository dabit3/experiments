name: gambit_court_core
description: Chess rules, notation, engine, and wire protocol shared by the Gambit Court server and clients.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
