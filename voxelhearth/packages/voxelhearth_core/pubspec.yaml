name: voxelhearth_core
description: Shared, platform-independent rules for Voxelhearth - blocks, items, recipes, world generation, the authoritative simulation and the JSON wire protocol.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
