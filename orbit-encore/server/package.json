{
  "name": "orbit-encore-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test *.test.mjs",
    "check": "node --check server.mjs && node --check engine.mjs && node --check charts.mjs",
    "assets": "node ../scripts/generate-assets.mjs"
  },
  "dependencies": {
    "ws": "8.21.3"
  },
  "engines": {
    "node": ">=22"
  }
}
