# security audit
## secrets scan (rg for key/token/password/secret literals)
(no literal secrets)

## files that would be committed under nitro-tots (no .env / keystores / credentials)
(none)

## server bind + input validation
packages/nitro_server/lib/src/server.dart:260:        s.error('unknown_type', 'Unknown message type "$type".');
packages/nitro_server/lib/src/server.dart:278:    return t.substring(0, math.min(14, t.length));
packages/nitro_server/lib/src/room.dart:116:    if (players.length >= settings.maxPlayers) return 'room_full';
packages/nitro_server/lib/src/room.dart:163:    if (patch['name'] is String && (patch['name'] as String).trim().isNotEmpty) {
packages/nitro_server/lib/src/room.dart:164:      p.name = (patch['name'] as String).trim().substring(0, math.min(14, (patch['name'] as String).trim().length));
packages/nitro_server/lib/src/room.dart:241:    final fieldSize = settings.fillBots ? settings.maxPlayers : players.length;
packages/nitro_server/lib/src/room.dart:249:      bots[slot] = BotDriver(slot: slot, seed: raceSeed, skill: (settings.botSkill - 0.15 + 0.3 * rng.nextDouble()).clamp(0.2, 1.0));

## dependency inventory
dependencies:
  flame: ^1.38.2
  flame_audio: ^2.12.2
  flutter:
    sdk: flutter
  go_router: ^18.0.1
  nitro_core:
    path: ../packages/nitro_core
  shared_preferences: ^2.5.5
  web_socket_channel: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

dependencies:
  meta: ^1.19.0
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
name: nitro_server
description: Authoritative multiplayer server for Nitro Tots.
version: 0.1.0

dependencies:
  args: ^2.7.0
  nitro_core:
    path: ../nitro_core
  path: ^1.9.0
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  shelf_web_socket: ^3.0.0
  web_socket_channel: ^3.0.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
## outdated
Showing outdated packages.
[*] indicates versions that are not the latest available.

Found no outdated packages
